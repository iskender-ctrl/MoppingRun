using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class sizeControl : MonoBehaviour
{
    
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
       // gameObject.transform.localScale += new Vector3(, transform.localScale.y, transform.localScale.z);
    }
}
